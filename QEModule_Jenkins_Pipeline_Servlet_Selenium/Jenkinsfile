pipeline {
	agent any
	
	stages {
	
		stage ('Compile Stage') {
		
			steps {
					sh '''
					cd QEModule_Jenkins_Pipeline_Servlet_Selenium
					$MVN_PATH/mvn clean compile
					'''
			}
		}

		stage ('Deployment Stage') {
			
			steps {
					sh '''
					cd QEModule_Jenkins_Pipeline_Servlet_Selenium
					$MVN_PATH/mvn install -DskipTests tomcat7:deploy'''
			}
			
		}
		
		stage ('Testing Stage') {
			
			steps {
					sh '''
					cd QEModule_Jenkins_Pipeline_Servlet_Selenium
					$MVN_PATH/mvn test'''
			}
		}
	}
}